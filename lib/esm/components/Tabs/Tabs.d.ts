import React, { ReactNode } from 'react';
interface TabsProps {
    children: ReactNode;
    defaultActive: string;
    className?: string;
}
export declare const Tabs: React.FC<TabsProps>;
export {};
