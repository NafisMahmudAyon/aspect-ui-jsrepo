import CodeSnippet from '@/components/CodeSnippet'
import EditPage from '@/components/EditPage'
import { importInNext, importInReact } from './Code/Code.tsx'
import IntroductionStepsForReact from './IntroductionStepsForReact'
import IntroductionStepsForNext from './IntroductionStepsForNext'



## Vite React Application

Setting Up Aspect UI in Vite React Application

<IntroductionStepsForReact />

Congratulations! You've now successfully set up Aspect UI in your Vite React application. You can begin using components from <span className="code">`aspect-ui`</span> in your project.

<CodeSnippet styles="mt-4" content={importInReact} lang="javascript" />

## Next JS Application

You can easily integrate <span className="code">`aspect-ui`</span> library into your Next.js application.

<IntroductionStepsForNext />

Now you can import any component from <span className="code">`aspect-ui`</span> and start using it in your NextJS project.

<CodeSnippet styles="mt-4" content={importInNext} lang="javascript" />
